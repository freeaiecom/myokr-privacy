name: Deploy Privacy Policy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Create index.html
        run: |
          cat > index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="zh-CN">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>MyOKR 隐私政策</title>
              <style>
                  body {
                      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
                      line-height: 1.6;
                      max-width: 800px;
                      margin: 40px auto;
                      padding: 20px;
                      color: #333;
                  }
                  h1 {
                      color: #0066cc;
                      border-bottom: 2px solid #0066cc;
                      padding-bottom: 10px;
                  }
                  h2 {
                      color: #444;
                      margin-top: 30px;
                  }
                  strong {
                      color: #0066cc;
                  }
                  .contact {
                      background: #f5f5f5;
                      padding: 20px;
                      border-radius: 8px;
                      margin-top: 20px;
                  }
                  .footer {
                      margin-top: 40px;
                      padding-top: 20px;
                      border-top: 1px solid #ddd;
                      text-align: center;
                      color: #666;
                  }
              </style>
          </head>
          <body>
              <h1>MyOKR 隐私政策</h1>
              <p><strong>生效日期</strong>：2026年2月13日</p>

              <h2>引言</h2>
              <p>本应用（"应用"）尊重并保护您的隐私。我们不会收集、存储或共享您的个人数据。</p>

              <h2>数据收集</h2>
              <p><strong>本应用不收集任何个人数据。</strong></p>
              <p>所有 OKR、目标、任务和记录都存储在您的设备本地，完全由您控制。</p>

              <h2>数据使用</h2>
              <p><strong>我们不会访问、使用或共享您的数据。</strong></p>
              <p>本应用的所有功能都运行在本地，不需要网络连接来处理您的数据。</p>

              <h2>数据共享</h2>
              <p><strong>我们不会与任何第三方共享您的数据。</strong></p>
              <p>本应用不会与任何第三方服务或平台共享您的信息。</p>

              <h2>联系我们</h2>
              <div class="contact">
                  <p><strong>如果您有任何疑问或建议，请通过以下方式联系我们：</strong></p>
                  <ul>
                      <li>📧 邮箱：<a href="mailto:liuxi686@126.com">liuxi686@126.com</a></li>
                  </ul>
              </div>

              <h2>政策更新</h2>
              <p>我们可能会偶尔更新本隐私政策。如有重大变更，我们会在应用中通知您。</p>

              <div class="footer">
                  <p><strong>感谢您使用 MyOKR！</strong></p>
              </div>
          </body>
          </html>
          EOF

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
